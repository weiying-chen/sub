# TODO

1. Wire CodeMirror to `analyzeTextByType` when ready to switch the editor path.
2. Define a concrete news structure and lock it down with stricter `parseNews` tests.
3. Add targeted tests for remaining gaps (editor integration, news edge cases).
